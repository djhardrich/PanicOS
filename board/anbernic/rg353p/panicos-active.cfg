# PanicOS active image selector.
# Edit IMAGE= to switch which squashfs the initramfs loads on next boot.
# The named file must exist on the boot vfat partition next to this file.
IMAGE=panicos-rg353p-minimal.squashfs
