# RTL8821CS (rtw88_8821cs) on H700: disable deep low-power state.
# With lps_deep enabled the driver enters a sleep state the AP doesn't
# track; the device stays 802.11-associated but drops all IP traffic
# until the interface is cycled. disable_lps_deep=Y prevents this.
options rtw88_core disable_lps_deep=Y
